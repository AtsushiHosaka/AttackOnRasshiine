開発方法

- issue全体を見る。今やってるissueについては「今やっている」ラベルをつける。「今やっている」ラベルがついているissueは進めず、競合しないようにする。
- worktreeを使わない。実装はこのプロジェクトディレクトリである `two/AttackOnRasshiine` の中だけで進める。
- 実装後は Unity MCP / Computer Use / Editor Preview / 自動テストなど、該当する手段で動作確認する。
- UI変更はスクリーンショットを撮り、実画面で文字の重なり、色の破綻、情報過多、画面サイズ依存の崩れを確認する。
- Heat UIを使える箇所では、ボタン、入力欄、プログレスバー、パネルの見た目をHeat UIベースにする。独自の四角い背景だけで済ませない。
- 色は原則としてシアン、アイスブルー/コールドバイオレット、ダークネイビー、白系テキストに絞る。ピンク/マゼンタを主役色にしない。
- ボス戦は最重要画面として、開始待機、参加操作、行動選択、支援、チーム追撃、ターン進行、結果、前に映す画面への反映まで確認する。
- PRを出す。PR本文には検証内容とスクリーンショットの保存先を記載する。
- PRで終わったissueはcloseするようにPR本文に `Closes #...` を書く。
- 実装が終わったらPR一覧を見て、レビューがついているものを見る。対応必須のレビューがあれば直し、再度PRにpushする。
- レビューがない、または修正すべきレビューがなければ、PRをmergeし、closeする。
- これを繰り返し、なるべくissueを消す。
